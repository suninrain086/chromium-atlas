# chrome.automation API

Extension API for traversing the live AXTree from javascript.

## Roots

Notes for roots are still being expanded; this is a placeholder paragraph used by the chromium-atlas mock fixture set so that the title-list view always has descriptive copy to render.

### Walking the tree

Use `find()` and `findAll()`.
