# Dump-accessibility-tree tests

Expectation-based tests that snapshot the AXTree and compare to a baseline.

## How it works

Notes for how it works are still being expanded; this is a placeholder paragraph used by the chromium-atlas mock fixture set so that the title-list view always has descriptive copy to render.

## Updating baselines

Pass --rebase to regenerate.
