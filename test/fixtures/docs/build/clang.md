# Building with clang

Chromium ships its own clang toolchain in third_party/llvm-build.

## Updating the toolchain

Notes for updating the toolchain are still being expanded; this is a placeholder paragraph used by the chromium-atlas mock fixture set so that the title-list view always has descriptive copy to render.

```sh
tools/clang/scripts/update.py
```

### Local clang builds

Notes for local clang builds are still being expanded; this is a placeholder paragraph used by the chromium-atlas mock fixture set so that the title-list view always has descriptive copy to render.
