# Component build

is_component_build splits chromium into many shared libraries for faster incremental linking.

## Trade-offs

Notes for trade-offs are still being expanded; this is a placeholder paragraph used by the chromium-atlas mock fixture set so that the title-list view always has descriptive copy to render.

### When to disable

For perf testing, set `is_component_build = false`. See [overview](overview.md).
