# Linking with lld

lld is the default linker. Notes on debugging linker issues, especially for thin-LTO builds.

## Debugging

Notes for debugging are still being expanded; this is a placeholder paragraph used by the chromium-atlas mock fixture set so that the title-list view always has descriptive copy to render.
