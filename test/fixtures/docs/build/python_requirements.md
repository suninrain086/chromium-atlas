# Python requirements

All build-time python tooling targets python3 ≥ 3.9.

## vpython

Notes for vpython are still being expanded; this is a placeholder paragraph used by the chromium-atlas mock fixture set so that the title-list view always has descriptive copy to render.

```python
import sys
assert sys.version_info >= (3, 9), 'Python 3.9+ required'
```

### Cipd packages

Notes for cipd packages are still being expanded; this is a placeholder paragraph used by the chromium-atlas mock fixture set so that the title-list view always has descriptive copy to render.
