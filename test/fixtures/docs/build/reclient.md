# Distributed builds with reclient

Reclient is the supported distributed build tool. Goma is deprecated.

## Setup

Notes for setup are still being expanded; this is a placeholder paragraph used by the chromium-atlas mock fixture set so that the title-list view always has descriptive copy to render.
