# Mojo IPC

Mojo is chromium's IPC system. All process boundaries cross via Mojo interfaces.

## Interface definition

Notes for interface definition are still being expanded; this is a placeholder paragraph used by the chromium-atlas mock fixture set so that the title-list view always has descriptive copy to render.

```cpp
interface Foo {
  DoBar(int32 x) => (string result);
};
```

### Bindings

Notes for bindings are still being expanded; this is a placeholder paragraph used by the chromium-atlas mock fixture set so that the title-list view always has descriptive copy to render.
