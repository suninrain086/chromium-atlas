# Security model

Defense in depth: sandboxing, site isolation, and the rule of two.

## Rule of two

See also: [site isolation](site_isolation.md), [linux sandbox](../linux/sandboxing.md).

### Privilege separation

Notes for privilege separation are still being expanded; this is a placeholder paragraph used by the chromium-atlas mock fixture set so that the title-list view always has descriptive copy to render.
