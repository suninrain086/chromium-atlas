# V8 integration

How blink embeds V8 — isolates, contexts, microtasks.

## Isolates

Notes for isolates are still being expanded; this is a placeholder paragraph used by the chromium-atlas mock fixture set so that the title-list view always has descriptive copy to render.

### Microtask checkpoints

Notes for microtask checkpoints are still being expanded; this is a placeholder paragraph used by the chromium-atlas mock fixture set so that the title-list view always has descriptive copy to render.
