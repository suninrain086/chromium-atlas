# cgroups and resource limits

How chromium interacts with linux cgroups, especially for renderer process priority.

## Renderer priority

Notes for renderer priority are still being expanded; this is a placeholder paragraph used by the chromium-atlas mock fixture set so that the title-list view always has descriptive copy to render.
