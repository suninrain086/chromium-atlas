# Linux perf profiling

Use perf and pprof to capture flamegraphs of chromium running under linux.

## Capture

Notes for capture are still being expanded; this is a placeholder paragraph used by the chromium-atlas mock fixture set so that the title-list view always has descriptive copy to render.
