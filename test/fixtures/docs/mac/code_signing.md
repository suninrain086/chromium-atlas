# Mac code signing

How chromium dev builds are codesigned for local execution.

## Self-signing

Notes for self-signing are still being expanded; this is a placeholder paragraph used by the chromium-atlas mock fixture set so that the title-list view always has descriptive copy to render.
