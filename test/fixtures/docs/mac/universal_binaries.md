# Universal binaries (mac arm64 + x86_64)

How chromium ships universal binaries for Apple Silicon and Intel macs.

## Lipo

Notes for lipo are still being expanded; this is a placeholder paragraph used by the chromium-atlas mock fixture set so that the title-list view always has descriptive copy to render.
