# The Only Doc Here

The Only Doc Here covers a focused slice of the chromium codebase. See also [keyboard](../accessibility/keyboard.md). See also [gn-check](../build/gn-check.md).

It is part of the **single-folder** area and pairs well with neighboring documents in the same folder.

## Background

This document describes part of the chromium codebase. See also [keyboard](../accessibility/keyboard.md). See also [gn-check](../build/gn-check.md).

## Usage

How to use this in day-to-day work. See also [keyboard](../accessibility/keyboard.md). See also [gn-check](../build/gn-check.md).

## Internals

Implementation details for those reading the source. See also [keyboard](../accessibility/keyboard.md). See also [gn-check](../build/gn-check.md).

### Subroutine

A worked example follows.

```js
// devtools-frontend snippet
export function frame(node) {
  return node && node.frameOwnerNodeId;
}
```

### Helper

And a second snippet:

```gn
static_library("example") {
  sources = [ "example.cc", "example.h" ]
  deps = [ "//base", "//content/public/browser" ]
}
```


_End of the only doc here._
