# Browser tests

Browser tests run in a real browser process — slower than unit tests but exercise the full stack.

## Fixtures

Notes for fixtures are still being expanded; this is a placeholder paragraph used by the chromium-atlas mock fixture set so that the title-list view always has descriptive copy to render.

```cpp
IN_PROC_BROWSER_TEST_F(MyBrowserTest, DoesThing) {
  EXPECT_TRUE(navigate_to_url(...));
}
```

### Async helpers

Notes for async helpers are still being expanded; this is a placeholder paragraph used by the chromium-atlas mock fixture set so that the title-list view always has descriptive copy to render.
