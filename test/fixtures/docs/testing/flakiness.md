# Flaky tests

Process for triaging and disabling flaky tests in chromium CI.

## Disabling

Notes for disabling are still being expanded; this is a placeholder paragraph used by the chromium-atlas mock fixture set so that the title-list view always has descriptive copy to render.
