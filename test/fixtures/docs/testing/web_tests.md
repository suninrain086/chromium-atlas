# Web tests (formerly layout tests)

Web platform tests that compare blink rendering output to expected baselines.

## Running

Notes for running are still being expanded; this is a placeholder paragraph used by the chromium-atlas mock fixture set so that the title-list view always has descriptive copy to render.

```sh
third_party/blink/tools/run_web_tests.py -t Default
```

### Rebaselining

Notes for rebaselining are still being expanded; this is a placeholder paragraph used by the chromium-atlas mock fixture set so that the title-list view always has descriptive copy to render.
