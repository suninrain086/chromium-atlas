# Windows accessibility

UI Automation provider implementation for chromium on Windows.

## UIA provider

Notes for uia provider are still being expanded; this is a placeholder paragraph used by the chromium-atlas mock fixture set so that the title-list view always has descriptive copy to render.

### MSAA fallback

See [the cross-platform accessibility overview](../accessibility/overview.md).
