# Windows build instructions

Build instructions for chromium on Windows. Requires Visual Studio 2022.

## VS setup

Notes for vs setup are still being expanded; this is a placeholder paragraph used by the chromium-atlas mock fixture set so that the title-list view always has descriptive copy to render.

### Building

See [build overview](../build/overview.md).
