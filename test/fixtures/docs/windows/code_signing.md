# Windows code signing

Authenticode signing for chromium installers.

## Stable channel

Notes for stable channel are still being expanded; this is a placeholder paragraph used by the chromium-atlas mock fixture set so that the title-list view always has descriptive copy to render.
