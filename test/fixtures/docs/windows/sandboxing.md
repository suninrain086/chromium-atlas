# Windows sandboxing

Job objects, restricted tokens, and the integrity-level model used to sandbox renderers on Windows.

## Job objects

Notes for job objects are still being expanded; this is a placeholder paragraph used by the chromium-atlas mock fixture set so that the title-list view always has descriptive copy to render.

### Integrity levels

Notes for integrity levels are still being expanded; this is a placeholder paragraph used by the chromium-atlas mock fixture set so that the title-list view always has descriptive copy to render.
